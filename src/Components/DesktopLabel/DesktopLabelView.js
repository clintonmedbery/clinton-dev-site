import React from "react";
// import styles from './DesktopLabel.module.css';
import { Label } from "react-desktop";

export const DesktopLabelView = ({labelTitle}) => {

    return (
        <div>
            {labelTitle}
        </div>

    );
};